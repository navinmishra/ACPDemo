const BASE=process.env.MERCHANT_API_URL!;
const KEY=process.env.MERCHANT_API_KEY!;
async function acpFetch<T>(path:string,init:RequestInit={}):Promise<T>{const r=await fetch(BASE+"/api"+path,{...init,headers:{"Content-Type":"application/json","Authorization":"Bearer "+KEY,"Idempotency-Key":crypto.randomUUID(),"Request-Id":crypto.randomUUID(),"API-Version":"2026-04-17",...(init.headers??{})}});if(!r.ok){const e=await r.json().catch(()=>({message:r.statusText}));throw new Error(e.message??"ACP error");}return r.json();}
export const acpClient={listProducts:()=>acpFetch<{products:any[]}>("/products"),createSession:(body:any)=>acpFetch<any>("/checkout_sessions",{method:"POST",body:JSON.stringify(body)}),updateSession:(id:string,body:any)=>acpFetch<any>("/checkout_sessions/"+id,{method:"POST",body:JSON.stringify(body)}),completeSession:(id:string,body:any)=>acpFetch<any>("/checkout_sessions/"+id+"/complete",{method:"POST",body:JSON.stringify(body)}),cancelSession:(id:string)=>acpFetch<any>("/checkout_sessions/"+id+"/cancel",{method:"POST"})};